version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: huston-housing_db
    restart: always
    command: postgres -c shared_preload_libraries=pg_stat_statements
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: huston-housing_db
    ports:
      - '5433:5432'
    volumes:
      - huston-housing_postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: huston-housing_redis
    restart: always
    ports:
      - '6379:6379'

volumes:
  huston-housing_postgres_data:
